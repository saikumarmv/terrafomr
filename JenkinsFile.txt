pipeline {
    agent any
    tools {
     terraform 'terraform'
    }
    
    stages {
       stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions :[], userRemoteConfig: [[url: "https://github.com/saikumarmv/terrafomr/tree/master"]]])
            }
        }
            stage("terraform init") {
            steps {
                bat '/usr/local/bin/terraform init'
                bat '/usr/local/bin/terraform version'
            }
        }
        stage("terraform Action") {
            steps {
                echo "Terraform action is --> ${action}"
                bat 'terraform plan --auto-approve'
            }
        }
    }
}
